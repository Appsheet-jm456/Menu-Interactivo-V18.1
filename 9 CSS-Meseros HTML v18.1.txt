<style>
/* ============================================
   CSS PANEL MESEROS V16 - CLOUDPYME POS FASE 7
   Completo y Corregido
============================================ */

/* VARIABLES */
:root {
  --primary: #1E40AF;
  --primary-light: #3B82F6;
  --primary-dark: #1E3A8A;
  --secondary: #0F172A;
  --secondary-light: #1E293B;
  --success: #10B981;
  --success-light: #D1FAE5;
  --warning: #F59E0B;
  --warning-light: #FEF3C7;
  --danger: #EF4444;
  --danger-light: #FEE2E2;
  --info: #06B6D4;
  --gray-50: #F9FAFB;
  --gray-100: #F3F4F6;
  --gray-200: #E5E7EB;
  --gray-300: #D1D5DB;
  --gray-400: #9CA3AF;
  --gray-500: #6B7280;
  --gray-600: #4B5563;
  --gray-700: #374151;
  --gray-800: #1F2937;
  --gray-900: #111827;
  --bg-main: #F1F5F9;
  --bg-card: #FFFFFF;
  --text-primary: #1F2937;
  --text-secondary: #6B7280;
  --border-color: #E5E7EB;
  --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
  --shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1);
  --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
  --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
  --shadow-xl: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
  --radius-sm: 8px;
  --radius: 12px;
  --radius-lg: 16px;
  --radius-xl: 20px;
  --radius-full: 50%;
  --transition: all 0.3s ease;
  --transition-fast: all 0.15s ease;
  --mesa-libre: #10B981;
  --mesa-ocupada: #EF4444;
  --mesa-reservada: #6B7280;
  --mesa-cuenta: #F59E0B;
}

/* RESET */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  font-size: 16px;
  scroll-behavior: smooth;
}

body {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  background: var(--bg-main);
  color: var(--text-primary);
  line-height: 1.5;
  overflow-x: hidden;
  min-height: 100vh;
}

/* ============================================
   LOGIN SCREEN
============================================ */
.login-screen {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
  padding: 20px;
}

.login-wrapper {
  width: 100%;
  max-width: 380px;
}

.login-card {
  background: var(--bg-card);
  border-radius: var(--radius-xl);
  padding: 40px 30px;
  box-shadow: var(--shadow-xl);
  text-align: center;
  animation: slideUp 0.5s ease;
}

@keyframes slideUp {
  from { opacity: 0; transform: translateY(30px); }
  to { opacity: 1; transform: translateY(0); }
}

.login-logo {
  margin-bottom: 20px;
}

.logo-icon {
  width: 80px;
  height: 80px;
  margin: 0 auto;
  background: linear-gradient(135deg, var(--primary), var(--primary-light));
  border-radius: var(--radius-lg);
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: var(--shadow-lg);
}

.logo-icon svg {
  width: 45px;
  height: 45px;
  color: white;
}

.login-title {
  font-size: 24px;
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: 4px;
}

.login-subtitle {
  font-size: 14px;
  color: var(--text-secondary);
  margin-bottom: 30px;
}

/* PIN DISPLAY */
.pin-display {
  margin-bottom: 25px;
}

.pin-dots {
  display: flex;
  justify-content: center;
  gap: 15px;
}

.pin-dot {
  width: 18px;
  height: 18px;
  border-radius: var(--radius-full);
  background: var(--gray-200);
  transition: var(--transition-fast);
}

.pin-dot.filled {
  background: var(--primary);
  transform: scale(1.1);
}

.pin-dot.error {
  background: var(--danger);
  animation: shake 0.3s ease;
}

@keyframes shake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-5px); }
  75% { transform: translateX(5px); }
}

/* PIN PAD */
.pin-pad {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12px;
  max-width: 280px;
  margin: 0 auto 20px;
}

.pin-key {
  width: 70px;
  height: 70px;
  border: 2px solid var(--gray-200);
  background: var(--bg-card);
  border-radius: var(--radius);
  font-size: 24px;
  font-weight: 600;
  color: var(--text-primary);
  cursor: pointer;
  transition: var(--transition-fast);
  display: flex;
  align-items: center;
  justify-content: center;
  user-select: none;
  -webkit-tap-highlight-color: transparent;
}

.pin-key:hover {
  border-color: var(--primary);
  background: var(--gray-50);
}

.pin-key:active {
  transform: scale(0.95);
  background: var(--primary-light);
  color: white;
}

.pin-key-delete {
  background: var(--danger-light);
  border-color: var(--danger);
  color: var(--danger);
}

.pin-key-delete:hover {
  background: var(--danger);
  color: white;
}

.pin-key-enter {
  background: var(--success);
  border-color: var(--success);
  color: white;
}

.pin-key-enter:hover {
  background: #059669;
}

.login-hint {
  font-size: 13px;
  color: var(--text-secondary);
  margin-bottom: 15px;
}

.login-error {
  display: none;
  background: var(--danger-light);
  color: var(--danger);
  padding: 12px;
  border-radius: var(--radius-sm);
  font-size: 14px;
  margin-top: 15px;
  border-left: 4px solid var(--danger);
}

.login-error.show {
  display: block;
  animation: shake 0.3s ease;
}

/* ============================================
   APP CONTAINER
============================================ */
.app-container {
  min-height: 100vh;
  background: var(--bg-main);
}

/* HEADER */
.app-header {
  background: var(--bg-card);
  padding: 12px 16px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  box-shadow: var(--shadow);
  position: sticky;
  top: 0;
  z-index: 100;
}

.header-left {
  display: flex;
  align-items: center;
  gap: 12px;
}

.header-logo {
  width: 40px;
  height: 40px;
  border-radius: var(--radius-sm);
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--primary);
}

.header-logo img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.header-logo-fallback {
  font-size: 24px;
}

.header-info {
  display: flex;
  flex-direction: column;
}

.header-title {
  font-size: 16px;
  font-weight: 700;
  color: var(--text-primary);
}

.header-user {
  font-size: 12px;
  color: var(--text-secondary);
}

.header-user strong {
  color: var(--primary);
}

.header-right {
  display: flex;
  align-items: center;
  gap: 8px;
}

.header-time {
  text-align: right;
  margin-right: 8px;
  display: none;
}

.header-time span {
  display: block;
}

.header-time span:first-child {
  font-size: 18px;
  font-weight: 700;
  color: var(--text-primary);
}

.header-time span:last-child {
  font-size: 11px;
  color: var(--text-secondary);
}

.btn-refresh,
.btn-logout {
  width: 40px;
  height: 40px;
  border: none;
  border-radius: var(--radius-sm);
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: var(--transition-fast);
}

.btn-refresh {
  background: var(--gray-100);
  color: var(--text-secondary);
}

.btn-refresh:hover {
  background: var(--primary);
  color: white;
}

.btn-logout {
  background: var(--danger-light);
  color: var(--danger);
}

.btn-logout:hover {
  background: var(--danger);
  color: white;
}

/* ACTION BAR */
.action-bar {
  padding: 16px;
  display: flex;
  gap: 12px;
}

.action-btn {
  flex: 1;
  padding: 16px 20px;
  border: none;
  border-radius: var(--radius);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  font-size: 16px;
  font-weight: 600;
  cursor: pointer;
  transition: var(--transition);
  box-shadow: var(--shadow);
}

.action-btn:active {
  transform: scale(0.98);
}

.action-domicilio {
  background: linear-gradient(135deg, #F97316, #EA580C);
  color: white;
}

.action-domicilio:hover {
  box-shadow: 0 4px 15px rgba(249, 115, 22, 0.4);
}

.action-rapido {
  background: linear-gradient(135deg, var(--primary-light), var(--primary));
  color: white;
}

.action-rapido:hover {
  box-shadow: 0 4px 15px rgba(59, 130, 246, 0.4);
}

.action-icon {
  font-size: 22px;
}

/* METRICS BAR */
.metrics-bar {
  padding: 0 16px 16px;
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 10px;
}

.metric-card {
  background: var(--bg-card);
  border-radius: var(--radius);
  padding: 12px;
  text-align: center;
  box-shadow: var(--shadow-sm);
  border-left: 4px solid var(--gray-300);
}

.metric-card.metric-libre {
  border-left-color: var(--mesa-libre);
}

.metric-card.metric-ocupada {
  border-left-color: var(--mesa-ocupada);
}

.metric-card.metric-reservada {
  border-left-color: var(--mesa-reservada);
}

.metric-value {
  display: block;
  font-size: 24px;
  font-weight: 700;
  color: var(--text-primary);
}

.metric-label {
  font-size: 11px;
  color: var(--text-secondary);
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

/* MESAS SECTION */
.mesas-section {
  padding: 0 16px 100px;
}

.mesas-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16px;
}

.mesas-header h2 {
  font-size: 18px;
  font-weight: 600;
  color: var(--text-primary);
}

.mesas-ubicacion {
  font-size: 12px;
  color: var(--text-secondary);
  background: var(--gray-100);
  padding: 4px 10px;
  border-radius: 20px;
}

.mesas-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
  gap: 12px;
}

/* MESA CARD */
.mesa-card {
  background: var(--bg-card);
  border-radius: var(--radius);
  padding: 16px;
  text-align: center;
  box-shadow: var(--shadow);
  cursor: pointer;
  transition: var(--transition);
  position: relative;
  overflow: hidden;
  border: 3px solid transparent;
}

.mesa-card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-lg);
}

.mesa-card:active {
  transform: scale(0.98);
}

.mesa-card.mesa-libre {
  border-color: var(--mesa-libre);
}

.mesa-card.mesa-libre::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: var(--mesa-libre);
}

.mesa-card.mesa-ocupada {
  border-color: var(--mesa-ocupada);
  background: linear-gradient(to bottom, #FEF2F2, white);
}

.mesa-card.mesa-ocupada::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: var(--mesa-ocupada);
}

.mesa-card.mesa-reservada {
  border-color: var(--mesa-reservada);
  background: var(--gray-50);
}

.mesa-card.mesa-reservada::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: var(--mesa-reservada);
}

.mesa-card.mesa-cuenta {
  border-color: var(--mesa-cuenta);
  background: linear-gradient(to bottom, #FFFBEB, white);
}

.mesa-card.mesa-cuenta::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: var(--mesa-cuenta);
}

.mesa-nombre {
  font-size: 18px;
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: 4px;
}

.mesa-info {
  font-size: 12px;
  color: var(--text-secondary);
}

.mesa-total {
  font-size: 16px;
  font-weight: 700;
  color: var(--mesa-ocupada);
  margin-top: 8px;
}

.mesa-status {
  display: inline-block;
  padding: 2px 8px;
  border-radius: 10px;
  font-size: 10px;
  font-weight: 600;
  text-transform: uppercase;
  margin-top: 8px;
}

.mesa-libre .mesa-status {
  background: var(--success-light);
  color: var(--success);
}

.mesa-ocupada .mesa-status {
  background: var(--danger-light);
  color: var(--danger);
}

.mesa-reservada .mesa-status {
  background: var(--gray-200);
  color: var(--gray-600);
}

.mesa-cuenta .mesa-status {
  background: var(--warning-light);
  color: var(--warning);
}

/* LOADING MESAS */
.loading-mesas {
  text-align: center;
  padding: 40px;
  color: var(--text-secondary);
  grid-column: 1 / -1;
}

.loading-mesas .spinner {
  width: 40px;
  height: 40px;
  border: 4px solid var(--gray-200);
  border-top-color: var(--primary);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
  margin: 0 auto 15px;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

/* ============================================
   MODALES
============================================ */
.modal {
  display: none;
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.6);
  z-index: 1000;
  align-items: center;
  justify-content: center;
  padding: 16px;
  overflow-y: auto;
}

.modal.show {
  display: flex;
  animation: fadeIn 0.2s ease;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

.modal-content {
  background: var(--bg-card);
  border-radius: var(--radius-lg);
  width: 100%;
  max-height: 90vh;
  overflow-y: auto;
  box-shadow: var(--shadow-xl);
  animation: slideIn 0.3s ease;
}

.modal-sm { max-width: 400px; }
.modal-md { max-width: 500px; }
.modal-lg { max-width: 600px; }

.modal-fullscreen {
  max-width: 100%;
  height: 100vh;
  max-height: 100vh;
  border-radius: 0;
}

@keyframes slideIn {
  from { opacity: 0; transform: translateY(-20px); }
  to { opacity: 1; transform: translateY(0); }
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16px 20px;
  border-bottom: 1px solid var(--border-color);
  position: sticky;
  top: 0;
  background: var(--bg-card);
  z-index: 1;
}

.modal-header h3 {
  font-size: 18px;
  font-weight: 600;
}

.modal-header-info {
  display: flex;
  flex-direction: column;
}

.modal-badge {
  font-size: 12px;
  color: var(--text-secondary);
  margin-top: 2px;
}

.btn-close {
  width: 36px;
  height: 36px;
  border: none;
  background: var(--gray-100);
  border-radius: var(--radius-sm);
  font-size: 18px;
  cursor: pointer;
  transition: var(--transition-fast);
  display: flex;
  align-items: center;
  justify-content: center;
}

.btn-close:hover {
  background: var(--danger-light);
  color: var(--danger);
}

.modal-body {
  padding: 20px;
}

.modal-footer {
  display: flex;
  justify-content: flex-end;
  gap: 12px;
  padding: 16px 20px;
  border-top: 1px solid var(--border-color);
  background: var(--gray-50);
}

.modal-description {
  text-align: center;
  color: var(--text-secondary);
  margin-bottom: 20px;
}

/* BUTTONS */
.btn-primary {
  padding: 12px 24px;
  background: var(--primary);
  color: white;
  border: none;
  border-radius: var(--radius-sm);
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  transition: var(--transition-fast);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
}

.btn-primary:hover {
  background: var(--primary-dark);
}

.btn-secondary {
  padding: 12px 24px;
  background: white;
  color: var(--text-secondary);
  border: 2px solid var(--border-color);
  border-radius: var(--radius-sm);
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  transition: var(--transition-fast);
}

.btn-secondary:hover {
  background: var(--gray-100);
}

.btn-danger-outline {
  padding: 12px 24px;
  background: transparent;
  color: var(--danger);
  border: 2px solid var(--danger);
  border-radius: var(--radius-sm);
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  transition: var(--transition-fast);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
}

.btn-danger-outline:hover {
  background: var(--danger);
  color: white;
}

/* FORM ELEMENTS */
.form-group {
  margin-bottom: 16px;
}

.form-group label {
  display: block;
  font-size: 13px;
  font-weight: 600;
  color: var(--text-secondary);
  margin-bottom: 6px;
}

.form-group input,
.form-group textarea,
.form-group select {
  width: 100%;
  padding: 12px 14px;
  border: 2px solid var(--border-color);
  border-radius: var(--radius-sm);
  font-size: 15px;
  transition: var(--transition-fast);
  font-family: inherit;
}

.form-group input:focus,
.form-group textarea:focus {
  outline: none;
  border-color: var(--primary);
  box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
}

.input-readonly {
  background: var(--gray-50);
  color: var(--text-primary);
  cursor: not-allowed;
}

/* COMENSALES SELECTOR */
.comensales-selector {
  display: flex;
  align-items: center;
  gap: 12px;
  justify-content: center;
}

.btn-comensal {
  width: 44px;
  height: 44px;
  border: 2px solid var(--border-color);
  background: white;
  border-radius: var(--radius-sm);
  font-size: 24px;
  font-weight: 600;
  cursor: pointer;
  transition: var(--transition-fast);
}

.btn-comensal:hover {
  border-color: var(--primary);
  background: var(--primary);
  color: white;
}

.comensales-selector input {
  width: 60px;
  text-align: center;
  font-size: 24px;
  font-weight: 700;
}

/* GESTION MESA */
.gestion-actions {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 10px;
  margin-bottom: 20px;
}

.gestion-btn {
  padding: 16px 12px;
  border: 2px solid var(--border-color);
  background: white;
  border-radius: var(--radius);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6px;
  cursor: pointer;
  transition: var(--transition-fast);
  font-size: 13px;
  font-weight: 600;
}

.gestion-btn:hover {
  border-color: var(--primary);
  background: var(--gray-50);
}

.gestion-btn.gestion-btn-primary {
  background: var(--primary);
  border-color: var(--primary);
  color: white;
}

.gestion-btn.gestion-btn-primary:hover {
  background: var(--primary-dark);
}

.gestion-icon {
  font-size: 24px;
}

.gestion-pedido h4 {
  font-size: 14px;
  color: var(--text-secondary);
  margin-bottom: 12px;
}

.items-list {
  background: var(--gray-50);
  border-radius: var(--radius-sm);
  max-height: 200px;
  overflow-y: auto;
}

.item-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12px;
  border-bottom: 1px solid var(--border-color);
}

.item-row:last-child {
  border-bottom: none;
}

.item-info {
  flex: 1;
}

.item-nombre {
  font-weight: 600;
  font-size: 14px;
}

.item-detalle {
  font-size: 12px;
  color: var(--text-secondary);
}

.item-precio {
  font-weight: 700;
  color: var(--primary);
}

.pedido-totales {
  margin-top: 16px;
  padding: 12px;
  background: var(--gray-50);
  border-radius: var(--radius-sm);
}

.total-row {
  display: flex;
  justify-content: space-between;
  padding: 6px 0;
  font-size: 14px;
}

.total-row.total-final {
  border-top: 2px solid var(--border-color);
  margin-top: 8px;
  padding-top: 12px;
  font-size: 18px;
  font-weight: 700;
  color: var(--primary);
}

/* ============================================
   POS CONTAINER
============================================ */
.pos-container {
  display: flex;
  height: calc(100vh - 60px);
}

.pos-productos {
  flex: 1;
  display: flex;
  flex-direction: column;
  border-right: 1px solid var(--border-color);
  overflow: hidden;
}

.pos-search {
  padding: 12px;
  border-bottom: 1px solid var(--border-color);
  position: relative;
}

.pos-search .search-icon {
  position: absolute;
  left: 24px;
  top: 50%;
  transform: translateY(-50%);
  font-size: 16px;
}

.pos-search input {
  width: 100%;
  padding: 10px 10px 10px 40px;
  border: 2px solid var(--border-color);
  border-radius: var(--radius-sm);
  font-size: 14px;
}

.pos-categorias {
  display: flex;
  gap: 8px;
  padding: 12px;
  overflow-x: auto;
  border-bottom: 1px solid var(--border-color);
  flex-shrink: 0;
}

.pos-categoria {
  padding: 8px 16px;
  border: 2px solid var(--border-color);
  background: white;
  border-radius: 20px;
  font-size: 13px;
  font-weight: 600;
  cursor: pointer;
  white-space: nowrap;
  transition: var(--transition-fast);
}

.pos-categoria:hover {
  border-color: var(--primary);
}

.pos-categoria.active {
  background: var(--primary);
  border-color: var(--primary);
  color: white;
}

.pos-grid {
  flex: 1;
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(110px, 1fr));
  gap: 10px;
  padding: 12px;
  overflow-y: auto;
  align-content: start;
}

.pos-producto {
  background: white;
  border: 2px solid var(--border-color);
  border-radius: var(--radius);
  padding: 10px;
  text-align: center;
  cursor: pointer;
  transition: var(--transition-fast);
}

.pos-producto:hover {
  border-color: var(--primary);
  transform: scale(1.02);
}

.pos-producto:active {
  transform: scale(0.98);
}

.pos-producto-img {
  width: 50px;
  height: 50px;
  border-radius: var(--radius-sm);
  object-fit: cover;
  margin: 0 auto 6px;
  background: var(--gray-100);
}

.pos-producto-nombre {
  font-size: 11px;
  font-weight: 600;
  margin-bottom: 4px;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  line-height: 1.3;
  min-height: 28px;
}

.pos-producto-precio {
  font-size: 13px;
  font-weight: 700;
  color: var(--primary);
}

/* CARRITO POS */
.pos-carrito {
  width: 280px;
  display: flex;
  flex-direction: column;
  background: var(--gray-50);
}

.carrito-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12px 16px;
  border-bottom: 1px solid var(--border-color);
  background: white;
}

.carrito-header h4 {
  font-size: 16px;
  font-weight: 600;
}

.btn-limpiar {
  padding: 6px 12px;
  background: var(--danger-light);
  color: var(--danger);
  border: none;
  border-radius: var(--radius-sm);
  font-size: 12px;
  font-weight: 600;
  cursor: pointer;
}

.btn-limpiar:hover {
  background: var(--danger);
  color: white;
}

.carrito-items {
  flex: 1;
  overflow-y: auto;
  padding: 12px;
}

.carrito-vacio {
  text-align: center;
  padding: 40px 20px;
  color: var(--text-secondary);
}

.carrito-vacio span {
  font-size: 48px;
  display: block;
  margin-bottom: 10px;
  opacity: 0.3;
}

.carrito-item {
  background: white;
  border-radius: var(--radius-sm);
  padding: 10px;
  margin-bottom: 8px;
  box-shadow: var(--shadow-sm);
}

.carrito-item-header {
  display: flex;
  justify-content: space-between;
  align-items: start;
}

.carrito-item-nombre {
  font-size: 13px;
  font-weight: 600;
  flex: 1;
}

.carrito-item-eliminar {
  width: 24px;
  height: 24px;
  border: none;
  background: var(--danger-light);
  color: var(--danger);
  border-radius: 4px;
  cursor: pointer;
  font-size: 12px;
}

.carrito-item-detalle {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: 6px;
}

.carrito-item-cantidad {
  display: flex;
  align-items: center;
  gap: 6px;
}

.carrito-item-cantidad button {
  width: 24px;
  height: 24px;
  border: 1px solid var(--border-color);
  background: white;
  border-radius: 4px;
  cursor: pointer;
  font-weight: 600;
}

.carrito-item-cantidad span {
  font-weight: 600;
  min-width: 20px;
  text-align: center;
}

.carrito-item-precio {
  font-weight: 700;
  color: var(--primary);
}

.carrito-totales {
  padding: 12px 16px;
  border-top: 1px solid var(--border-color);
  background: white;
}

.btn-enviar-cocina {
  width: 100%;
  padding: 14px;
  background: var(--success);
  color: white;
  border: none;
  border-radius: var(--radius-sm);
  font-size: 16px;
  font-weight: 700;
  cursor: pointer;
  margin-top: 12px;
}

.btn-enviar-cocina:hover {
  background: #059669;
}

/* COBRAR */
.cobrar-resumen {
  margin-bottom: 20px;
}

.items-resumen {
  background: var(--gray-50);
  border-radius: var(--radius-sm);
  padding: 12px;
  max-height: 150px;
  overflow-y: auto;
  font-size: 13px;
}

.cobrar-totales {
  background: var(--gray-50);
  border-radius: var(--radius-sm);
  padding: 12px;
  margin-top: 12px;
}

.metodo-pago-section h4 {
  font-size: 14px;
  margin-bottom: 12px;
}

.metodos-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 10px;
  margin-bottom: 16px;
}

.metodo-btn {
  padding: 16px 12px;
  border: 2px solid var(--border-color);
  background: white;
  border-radius: var(--radius);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6px;
  cursor: pointer;
  transition: var(--transition-fast);
  font-size: 13px;
  font-weight: 600;
}

.metodo-btn:hover {
  border-color: var(--primary);
}

.metodo-btn.active {
  background: var(--primary);
  border-color: var(--primary);
  color: white;
}

.metodo-btn span:first-child {
  font-size: 24px;
}

.calculadora-efectivo {
  background: var(--gray-50);
  border-radius: var(--radius-sm);
  padding: 16px;
}

.calculadora-efectivo p {
  font-size: 13px;
  color: var(--text-secondary);
  margin-bottom: 12px;
}

.efectivo-botones {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 8px;
  margin-bottom: 12px;
}

.efectivo-botones button {
  padding: 10px;
  border: 2px solid var(--border-color);
  background: white;
  border-radius: var(--radius-sm);
  font-size: 13px;
  font-weight: 600;
  cursor: pointer;
}

.efectivo-botones button:hover {
  border-color: var(--primary);
}

.efectivo-botones button.active {
  background: var(--primary);
  border-color: var(--primary);
  color: white;
}

.calculadora-efectivo input {
  width: 100%;
  padding: 10px;
  border: 2px solid var(--border-color);
  border-radius: var(--radius-sm);
  font-size: 15px;
  text-align: center;
}

.devuelta-container {
  margin-top: 12px;
  padding: 12px;
  background: var(--success-light);
  border-radius: var(--radius-sm);
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.monto-devuelta {
  font-size: 20px;
  font-weight: 700;
  color: var(--success);
}

.btn-confirmar-pago {
  width: 100%;
  padding: 16px;
  font-size: 16px;
}

/* DOMICILIO */
.domicilio-cliente h4 {
  font-size: 14px;
  margin-bottom: 16px;
  color: var(--text-secondary);
}

.autocomplete-list {
  position: absolute;
  top: 100%;
  left: 0;
  right: 0;
  background: white;
  border: 2px solid var(--border-color);
  border-radius: var(--radius-sm);
  box-shadow: var(--shadow-lg);
  z-index: 10;
  max-height: 150px;
  overflow-y: auto;
}

.autocomplete-item {
  padding: 10px 12px;
  cursor: pointer;
  border-bottom: 1px solid var(--border-color);
}

.autocomplete-item:last-child {
  border-bottom: none;
}

.autocomplete-item:hover {
  background: var(--gray-50);
}

/* LOADING OVERLAY */
.loading-overlay {
  display: none;
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(255, 255, 255, 0.95);
  z-index: 2000;
  align-items: center;
  justify-content: center;
  flex-direction: column;
}

.loading-overlay.show {
  display: flex;
}

.spinner-large {
  width: 50px;
  height: 50px;
  border: 4px solid var(--gray-200);
  border-top-color: var(--primary);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
  margin-bottom: 16px;
}

/* TOAST */
.toast-container {
  position: fixed;
  top: 20px;
  right: 20px;
  z-index: 3000;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.toast {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 14px 20px;
  background: white;
  border-radius: var(--radius-sm);
  box-shadow: var(--shadow-lg);
  animation: toastIn 0.3s ease;
  min-width: 280px;
}

@keyframes toastIn {
  from { opacity: 0; transform: translateX(100px); }
  to { opacity: 1; transform: translateX(0); }
}

.toast.toast-out {
  animation: toastOut 0.3s ease forwards;
}

@keyframes toastOut {
  to { opacity: 0; transform: translateX(100px); }
}

.toast-icon {
  font-size: 24px;
}

.toast-content {
  flex: 1;
}

.toast-title {
  font-weight: 600;
  font-size: 14px;
}

.toast-message {
  font-size: 13px;
  color: var(--text-secondary);
}

.toast-success { border-left: 4px solid var(--success); }
.toast-error { border-left: 4px solid var(--danger); }
.toast-warning { border-left: 4px solid var(--warning); }
.toast-info { border-left: 4px solid var(--info); }

/* ============================================
   RESPONSIVE
============================================ */
@media (min-width: 768px) {
  .header-time { display: block; }
  .metrics-bar { grid-template-columns: repeat(4, 1fr); }
  .mesas-grid { grid-template-columns: repeat(auto-fill, minmax(160px, 1fr)); }
}

@media (max-width: 768px) {
  .pos-container { flex-direction: column; }
  .pos-carrito { width: 100%; max-height: 45vh; }
  .pos-productos { max-height: 55vh; }
  .gestion-actions { grid-template-columns: 1fr; }
  .metodos-grid { grid-template-columns: 1fr; }
  .modal-fullscreen { border-radius: 0; }
  .pos-grid { grid-template-columns: repeat(auto-fill, minmax(90px, 1fr)); }
}

@media (max-width: 480px) {
  .action-bar { flex-direction: column; }
  .metrics-bar { grid-template-columns: repeat(2, 1fr); }
  .pin-key { width: 60px; height: 60px; font-size: 20px; }
  .login-card { padding: 30px 20px; }
  .efectivo-botones { grid-template-columns: repeat(2, 1fr); }
}
</style>